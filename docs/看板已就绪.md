# 看板已就绪 — 你几乎不用做任何事

Agent 已自动完成：

- [x] 合并代码到 `main`
- [x] 56 个站点全部接入 PV / 结账 / 付费埋点
- [x] 静态看板 HTML 提交到仓库，经 **jsDelivr CDN** 全球加速
- [x] GitHub Actions 每日更新 SEO 与汇总数据

## 你要做的：0 步（默认）

浏览器打开（**请收藏此地址**）：

**https://cdn.jsdelivr.net/gh/Waitind123/daily-sites-factory@main/dashboard-public/index.html**

即可看到所有站点的 PV、UV、SEO 分、转化漏斗。

> 请勿使用 `factory-dashboard.vercel.app`（他人站点）。`daily-sites-analytics.vercel.app` 需等 Vercel 每日部署额度恢复后才会上线。

数据每次 SEO 日报或 `analytics/rollup.json` 更新后会自动刷新看板（jsDelivr 缓存约数分钟）。

---

## 仅当 PV 实时 +1 不生效时（可选）

看板展示走 jsDelivr；**实时埋点**需要 Vercel 上的 `daily-sites-analytics` API。若 Vercel 报每日 100 次部署上限，等 24 小时后再在 Actions 运行「单独部署看板到 Vercel」。

---

## 和飞书机器人的关系

| 功能 | 你要管吗 |
|------|----------|
| 飞书 @机器人 发指令 | 之前配过 feishu-bot 的继续用 |
| 全站数据看板 | **不用管**，打开上方 URL 即可 |

有问题直接把看板截图发群里即可。
