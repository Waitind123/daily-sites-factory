# 看板已就绪 — 你几乎不用做任何事

Agent 已自动完成：

- [x] 合并代码到 `main`
- [x] 56 个站点全部接入 PV / 结账 / 付费埋点
- [x] 静态看板通过 **GitHub Pages** 自动部署（不依赖 Vercel 域名）
- [x] GitHub Actions 每日更新 SEO 与汇总数据

## 你要做的：0 步（默认）

浏览器打开（**请收藏此地址**）：

**https://waitind123.github.io/daily-sites-factory/**

即可看到所有站点的 PV、UV、SEO 分、转化漏斗。

> 请勿使用 `factory-dashboard.vercel.app`（他人站点）或尚未部署的 `daily-sites-analytics.vercel.app`。

数据每次 SEO 日报或 `analytics/rollup.json` 更新后会自动刷新看板。

---

## 仅当 PV 实时 +1 不生效时（可选）

看板展示走 GitHub Pages；**实时埋点**需要 Vercel 上的 `daily-sites-analytics` API。若未部署：

1. 在 GitHub 仓库 **Settings → Secrets** 配置 `VERCEL_TOKEN`
2. **Actions** → **单独部署看板到 Vercel** → **Run workflow**

完成后各站访问会自动上报到看板数据文件。

---

## 和飞书机器人的关系

| 功能 | 你要管吗 |
|------|----------|
| 飞书 @机器人 发指令 | 之前配过 feishu-bot 的继续用 |
| 全站数据看板 | **不用管**，打开上方 URL 即可 |

有问题直接把看板截图发群里即可。
