# 看板已就绪 — 你几乎不用做任何事

## 看板地址（请收藏）

**https://waitind123.github.io/daily-sites-factory/**

深色页面，标题「全站运营看板」，56 站 SEO / PV / 转化一览。

> 不要用 jsDelivr 链接（浏览器会显示 HTML 源码）。  
> 不要用 `factory-dashboard.vercel.app`（他人站点）。

若首次打开 404：仓库 **Settings → Pages → Build and deployment → Source** 选 **GitHub Actions**（仅需一次）。

---

## Vercel 部署上限（免费版约 100 次/天）

近几天实际部署量（workflow 统计）：

| 日期 | 约部署次数 |
|------|-----------|
| 6/25 | ~52 次 |
| 6/26 | ~28 次 |
| 6/27 | ~31 次 |
| 6/28 | ~26 次 |
| 6/29 | **~146 次**（含批量同步埋点触发的全站重部署，上午触顶） |

**已自动优化**：仅改埋点配置（`analytics-client.ts` 等）不再触发 56 站全量重部署；达上限后 CI 会跳过，次日再部署。

本地查看统计：`node scripts/vercel-deploy-stats.mjs`

---

## 你要做的：0 步（默认）

打开上方看板地址即可。SEO 数据每日自动更新。

PV 实时 +1 需 Vercel 看板 API（`daily-sites-analytics`），额度恢复后 Actions 会自动部署。
